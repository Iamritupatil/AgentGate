# Optional: copy to backend/.env. Defaults work without this file.
AGENTGATE_APP_NAME=AgentGate
AGENTGATE_ENVIRONMENT=development
# A JSON array; origins must match the browser's scheme, host and port.
AGENTGATE_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
